import { TelaPlaceholder } from '@/components/workspace/TelaPlaceholder'

/**
 * Objetivos — metas (valor alvo, data, acumulado, necessário/mês, progresso).
 * Conteúdo real no Spec 05.
 */
export default function ObjetivosPage() {
  return (
    <TelaPlaceholder
      titulo="Objetivos"
      descricao="Metas financeiras: valor alvo, acumulado, necessário por mês e progresso."
      spec="Spec 05"
    />
  )
}
