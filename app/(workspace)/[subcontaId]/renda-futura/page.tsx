import { TelaPlaceholder } from '@/components/workspace/TelaPlaceholder'

/**
 * Renda Futura — projeção de aposentadoria (juros compostos, patrimônio
 * necessário). Conteúdo real no Spec 06+.
 */
export default function RendaFuturaPage() {
  return (
    <TelaPlaceholder
      titulo="Renda Futura"
      descricao="Projeção de aposentadoria por juros compostos e patrimônio necessário para a renda passiva desejada."
      spec="Spec 06"
    />
  )
}
